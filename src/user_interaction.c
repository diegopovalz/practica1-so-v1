#include "../include/user_interaction.h"
#include <stdio.h>

int get_node(int node_capacity){
    int *node = 0;
    printf("Please insert the node between [0-%i] to analyze", node_capacity - 1);
    scanf("%i", node);
    return 0;

}
